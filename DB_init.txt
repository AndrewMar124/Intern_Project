connect: 

docker ps 
docker exec -it [CONTAINER ID] bash
psql -U user -d exampledb

create tables:

CREATE TABLE Question( qID INT, uName VARCHAR(255), qData VARCHAR(255));
CREATE TABLE Response( rID INT, rData VARCHAR(255)); 

fill response table:

INSERT INTO Response (rID, rData)
VALUES (1, 'test response'), (2, 'test response, but second...');